// RecordingIndicator.swift
// Gestion Travaux
//
// Shell component — activated in Story 2.2 to display a waveform animation
// while the BigButton is green and audio is being captured.
// Hidden in this story.

import SwiftUI

struct RecordingIndicator: View {
    var body: some View {
        // Shell: hidden until Story 2.2 implements the audio waveform animation.
        EmptyView()
    }
}
